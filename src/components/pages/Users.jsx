import styled from "styled-components";

export const Users = () => {
  return (
    <SContaner>
      <h2>ユーザー一覧</h2>
    </SContaner>
  );
};

const SContaner = styled.div`
  text-align: center;
`;
